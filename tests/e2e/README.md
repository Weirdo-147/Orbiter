# End-to-end tests

Put full workflow and environment-level tests in this folder.

E2E tests are expected to run in dedicated environments (for example nightly pipelines or release gates), not on every pull request.
